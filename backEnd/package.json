{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env ./src/server.js",
    "build": "cd ../frontEnd && npm install && npm run build && cd ../backEnd && npm install",
    "start": "node ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Joshc",
  "license": "ISC",
  "description": "Full stack bike rental shop React, Bootstrap, Node.js, Express, Sqlite, jwt, bcrypt.js",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
